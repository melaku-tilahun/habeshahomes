# Rate limiting definitions for HabeshaHomes API (HTTP context)
limit_req_zone $binary_remote_addr zone=api:10m rate=60r/m;
limit_req_zone $binary_remote_addr zone=webhooks:10m rate=120r/m;
limit_req_status 429;
